# ===================================================================
# LOCAL DEVELOPMENT OVERRIDES
# ===================================================================
# Copy this file to .env.local and customize for your local environment
# The .env.local file is ignored by git
#
# Usage: docker-compose -f docker-compose.yml -f docker-compose.dev.yml up
# Or uncomment the line below to automatically use dev mode:

# COMPOSE_FILE=docker-compose.yml:docker-compose.dev.yml

# ===================================================================
# LOCAL DEVELOPMENT PATHS
# ===================================================================
# Override with your local Calibre library path
CALIBRE_LIBRARY_PATH=/Users/khanhdo/dev/calibre-web-data/calibre-library

# Local database (optional - you can use the production DB for testing)
# DATABASE_URL=postgresql+asyncpg://calibre:password@localhost:5432/calibre_web

# Local Redis (optional)
# REDIS_URL=redis://localhost:6379

# Frontend dev server port
FRONTEND_PORT=3001
